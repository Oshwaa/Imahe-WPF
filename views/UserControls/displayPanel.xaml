<UserControl x:Class="Imahe.views.UserControls.displayPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Imahe.views.UserControls"
             xmlns:models="clr-namespace:Imahe.models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <models:MainViewModel/>
    </UserControl.DataContext>

    <Grid>
        <ScrollViewer Grid.Row="1">
            <WrapPanel x:Name="ImagePanel" Orientation="Horizontal">
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
